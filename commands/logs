import { db } from '../ghettodb';
import fs from 'fs';

export default async function(req, res) {
  let id = req.param.id;
  console.log('log id', id)
  if (!id) {
    return res.json({ message: 'No id RIP' });
  }
  res.json('/mozart/'+fs.readFileSync(id+'.log'));
}